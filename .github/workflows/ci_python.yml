# Generated by GhostCMD (ci gen)
# ghost-intent: "python matrix + black + pytest + coverage + docker build + security scan + sbom + codeql"
# ghost-plan-sha: 2ff7d109ae6dae4b11cc12700d25bffa26167f5527c0ae7dcaf4ec57188830ea
name: CI (Generated by GhostCMD)
'on':
  push:
    branches:
    - main
  pull_request: {}
  workflow_dispatch: {}
env:
  ENV: dev
  REGISTRY: ghcr.io
  IMAGE: ghcr.io/${{ github.repository }}:${{ github.sha }}
  RELEASE_NAME: ghostcmd-app
jobs:
  lint:
    name: lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: pip-${{ runner.os }}-${{ hashFiles('**/requirements.txt') }}
          restore-keys: pip-${{ runner.os }}-
      - run: python -m pip install --upgrade pip black
      - run: black --check .
  test:
    name: test
    runs-on: ubuntu-latest
    needs: [lint]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: pip-${{ runner.os }}-${{ hashFiles('**/requirements.txt') }}
          restore-keys: pip-${{ runner.os }}-
      - run: python -m pip install --upgrade pip pytest pytest-cov
      - run: pytest -q --maxfail=1 --disable-warnings --cov=. --cov-report=xml
      - uses: actions/upload-artifact@v4
        with:
          name: coverage-xml
          path: coverage.xml
  test_matrix:
    name: test_matrix
    runs-on: ubuntu-latest
    strategy:
      matrix: {'python-version': ['3.8', '3.9', '3.10', '3.11']}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '${{ matrix.python-version }}'
      - uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: pip-${{ runner.os }}-${{ hashFiles('**/requirements.txt') }}
          restore-keys: pip-${{ runner.os }}-
      - run: python -m pip install --upgrade pip
      - run: pip install -r requirements.txt || true
      - run: pytest -q --maxfail=1 --disable-warnings || true
  security_scan:
    name: security_scan
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo 'skip unsupported step type: security_scan'
  sbom:
    name: sbom
    runs-on: ubuntu-latest
    needs: [lint, test, test_matrix, security_scan]
    steps:
      - uses: actions/checkout@v4
      - run: echo 'skip unsupported step type: sbom'
  codeql:
    name: codeql
    runs-on: ubuntu-latest
    needs: [lint, test, test_matrix, security_scan, sbom]
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y jq
      - run: git init . && git fetch origin main --depth=1 && git checkout main || true
      - run: echo '::group::Initialize CodeQL'
      - run: codeql database create codeql-db --language=cpp,java,python,go,ruby,php,csharp --command='make build || true' || echo 'skip codeql db'
      - run: codeql database analyze codeql-db --format=sarifv2.1.0 --output=results.sarif || true
      - uses: actions/upload-artifact@v4
        with:
          name: codeql-results
          path: results.sarif
  docker_build:
    name: docker_build
    runs-on: ubuntu-latest
    needs: [lint, test, test_matrix, security_scan, sbom, codeql]
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3
      - name: Docker login
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GHCR_TOKEN }}
      - name: Docker build & push
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          file: Dockerfile
          tags: |
            ghcr.io/${{ github.repository }}:sha-${{ github.sha }}