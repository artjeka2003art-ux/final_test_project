# Generated by GhostCMD (ci gen)
# ghost-intent: "python + black + pytest"
# ghost-plan-sha: 503ec0960e643508722f663a5c1a0e8ed9f339f6c9d21d64b72194a6dbbe3f54
name: CI (Generated by GhostCMD)
'on':
  push:
    branches:
    - main
  pull_request: {}
  workflow_dispatch: {}
env:
  ENV: dev
  REGISTRY: ghcr.io
  IMAGE: ghcr.io/${{ github.repository }}:${{ github.sha }}
  RELEASE_NAME: ghostcmd-app
jobs:
  lint:
    name: lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: pip-${{ runner.os }}-${{ hashFiles('**/requirements.txt') }}
          restore-keys: pip-${{ runner.os }}-
      - run: python -m pip install --upgrade pip black
      - run: black --check .
  test:
    name: test
    runs-on: ubuntu-latest
    needs: [lint]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: pip-${{ runner.os }}-${{ hashFiles('**/requirements.txt') }}
          restore-keys: pip-${{ runner.os }}-
      - run: python -m pip install --upgrade pip pytest pytest-cov
      - run: pytest -q --maxfail=1 --disable-warnings --cov=. --cov-report=xml
      - uses: actions/upload-artifact@v4
        with:
          name: coverage-xml
          path: coverage.xml
  docker_build:
    name: docker_build
    runs-on: ubuntu-latest
    needs: [lint, test]
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3
      - name: Docker login
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GHCR_TOKEN }}
      - name: Docker build & push
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          file: Dockerfile
          tags: |
            ghcr.io/${{ github.repository }}:sha-${{ github.sha }}